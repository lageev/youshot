# YouShot

macOS 菜单栏截图应用。支持延迟截图、区域选取、多屏、自定义快捷键，并以显示器原生像素 + 无损 PNG 输出。

## 运行

Xcode：

```bash
open YouShot.xcodeproj
```

选择共享的 `YouShot` Scheme，运行目标选择 `My Mac`，然后按 `⌘R`。

命令行构建：

```bash
./scripts/build.sh
open dist/YouShot.app
```

首次使用需在「系统设置 > 隐私与安全性 > 屏幕录制」中授权。

## 功能

- 当前屏幕 / 所有屏幕 / 区域选取
- 区域模式：先选区 → 再延迟 → 截图 → **在截图原位置**弹出标注层（不新开窗口）
- 区域选取支持窗口吸附（悬停高亮、单击选取；拖拽自由框选）
- 吸附到窗口时按窗口采集（背景透明、不含系统投影），避免圆角处出现深边
- 导出为透明底 PNG，圆角与阴影（模糊、深浅）可在设置中调节
- 标注工具：矩形 / 直线 / 箭头 / 画笔 / 文字 / 高亮 / 马赛克 / 模糊 / 水印
- 绘制类工具共用可调颜色（8 色）与粗细，文字字号跟随粗细
- 画笔支持硬笔 / 荧光笔 / 柔边，可调透明度；荧光笔为正片叠底，便于手绘高亮
- 画笔 / 直线 / 箭头按住 ⇧ 吸附为水平、垂直或 45° 直线
- 矩形为圆角描边；直线两端圆角
- 箭头为圆润曲线造型，预览与输出共用同一条路径
- 文字在点击位置直接键入（回车确认、Esc 取消），不弹新窗口
- Esc 可取消选区 / 倒计时截图；标注层 Esc 取消编辑（文字输入中则先取消输入）
- 高亮：多个区域共用一层遮罩（重叠不加深），深浅可实时调整，导出时才合入图片
- 水印：输入文字后点「应用」叠加一次，可调文字大小 / 颜色 / 透明度，预设斜向平铺 / 居中大字 / 右下角 / 底部横幅
- 截图取消或完成后不自动弹出主窗口，只有菜单「设置…」才打开
- 模糊强度与羽化宽度可在设置/编辑窗调节并持久化
- 延迟倒计时（0–60 秒）
- 全局快捷键可自定义（默认 `⌥⇧⌘3` / `⌥⇧⌘4` / `⌥⇧⌘5`）
- 菜单栏为主入口，设置为多标签窗口（截图 / 标注 / 水印 / 导出 / 快捷键）

## 结构

- `YouShotApp.swift` — 菜单栏、设置窗、热键注册
- `ContentView.swift` — 多标签设置界面与快捷键录制
- `CaptureController.swift` — 倒计时、采集、保存、编辑状态与标注样式
- `AnnotationOverlay.swift` — 截图原位置标注层（NSPanel）
- `EditorView.swift` — 标注画布、样式条与工具条、内联文字输入
- `AnnotationRenderer.swift` — 工具定义、调色板、矩形/箭头/画笔/文字/高亮/水印绘制
- `RegionSelector.swift` — 区域选取遮罩
- `HotKeyManager.swift` — Carbon 全局热键
- `KeyChord.swift` — 快捷键模型与持久化
- `AppSettings.swift` — 颜色/粗细/笔刷/透明度/高亮/水印（文字/样式/大小/颜色/透明度）/模糊/羽化/吸附等偏好
- `ImageMosaic.swift` — 马赛克、模糊与 PNG 写入
- `CaptureMode.swift` — 截图模式

## 质量约定

截图使用 ScreenCaptureKit：输出尺寸为 `contentRect/sourceRect × pointPixelScale`，`captureResolution = .best`，保存无损 PNG，并写入剪贴板。
